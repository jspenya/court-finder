<div class="result result--empty">
  <p class="result__empty-title">No open courts in this window</p>
  <p class="result__empty-detail">
    Nothing available <%= format_play_time_window(search) %>. Try a wider time range or another date.
    Checked at <%= format_checked_at(checked_at) %>.
  </p>
</div>
